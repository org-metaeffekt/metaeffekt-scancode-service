[tox]
env_list =
    py311
minversion = 4.12.1

[testenv]
description = run the tests with pytest
package = wheel
wheel_build_env = .pkg
deps =
    pytest>=8
    -r requirements.txt
commands =
    pytest {tty:--color=yes} {posargs}

[testenv:start]
description = start fastapi
package = wheel
wheel_build_env = .pkg
deps =
    -r requirements.txt
commands =
    python -m scancode_extensions.service
